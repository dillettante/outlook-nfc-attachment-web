# 기여 방법

1. 변경 범위를 작게 유지한다.
2. `npm test`와 `npm run validate:source`를 실행한다.
3. 인증서, 로그, 메일 원본, 사용자 절대경로를 커밋하지 않는다.
4. UI 또는 첨부 동작을 바꾸면 변경 이유와 Outlook 웹·Mac 데스크톱 확인 결과를 적는다.

25,000,000바이트 한도는 Outlook 첨부 API의 현재 안정성 경계다. 한도 변경은 실제 Outlook
웹·Mac 데스크톱 검증 결과와 함께 제안한다.
